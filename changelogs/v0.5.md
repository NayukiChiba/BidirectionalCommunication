# v0.5

## 新增

- 同时发布内置 FastAPI/SQLite 的完整版和只连接远程服务的纯客户端版。
- `python main.py` 自动生成认证密钥、迁移 SQLite 并启动单机服务。
- GitHub Actions 构建 sidecar、执行冒烟测试并发布两个 NSIS 安装程序。

## 改进

- 完整版自动选择本地端口并等待内置后端就绪。
- 桌面主进程退出后自动终止 PyInstaller sidecar 进程树。
- 外部 PostgreSQL 和 Redis 保留为可选部署能力。
